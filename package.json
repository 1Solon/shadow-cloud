{
  "name": "shadow-cloud",
  "version": "0.7.0",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format",
    "pr": "turbo run format && turbo run lint && turbo run typecheck && turbo run test && turbo run build"
  },
  "devDependencies": {
    "turbo": "2.9.6",
    "typescript": "6.0.3"
  }
}